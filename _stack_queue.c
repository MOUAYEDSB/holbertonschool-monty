#include "monty.h"

/**
 * _stack - placeholder function for stack (LIFO) behavior
 * @stack: double pointer to the stack
 * @line_number: line number of the opcode
 */
void _stack(stack_t **stack, unsigned int line_number)
{
    /* Implement stack (LIFO) behavior here */
    (void)stack;
    (void)line_number;
}

/**
 * _queue - placeholder function for queue (FIFO) behavior
 * @stack: double pointer to the stack
 * @line_number: line number of the opcode
 */
void _queue(stack_t **stack, unsigned int line_number)
{
    /* Implement queue (FIFO) behavior here */
    (void)stack;
    (void)line_number;
}
